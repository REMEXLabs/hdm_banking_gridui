/*
 * Copyright 2017 Hochschule der Medien (HdM) / Stuttgart Media University
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at

 * http://www.apache.org/licenses/LICENSE-2.0

 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

/* -------------------Variablen------------------- */
var i;


/* -------------------Scanning------------------- */


function insert0(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[0].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[0].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert0);
    }
    else {
        document.removeEventListener('keypress', insert0);
    }
}
function insert1(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[1].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[1].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert1);
    }
    else {
        document.removeEventListener('keypress', insert1);
    }
}
function insert2(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[2].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[2].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert2);
    }
    else {
        document.removeEventListener('keypress', insert2);
    }
}
function insert3(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[3].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[3].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert3);
    }
    else {
        document.removeEventListener('keypress', insert3);
    }
}
function insert4(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[4].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[4].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert4);
    }
    else {
        document.removeEventListener('keypress', insert4);
    }
}
function insert5(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[5].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[5].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert5);
    }
    else {
        document.removeEventListener('keypress', insert5);
    }
}
function insert6(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[6].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[6].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert6);
    }
    else {
        document.removeEventListener('keypress', insert6);
    }
}
function insert7(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[7].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[7].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert7);
    }
    else {
        document.removeEventListener('keypress', insert7);
    }
}
function insert8(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[8].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[8].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert8);
    }
    else {
        document.removeEventListener('keypress', insert8);
    }
}
function insert9(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[9].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[9].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert9);
    }
    else {
        document.removeEventListener('keypress', insert9);
    }
}
function insert10(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[10].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[10].click();

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert10);
    }
    else {
        document.removeEventListener('keypress', insert10);
    }
}
function insert11(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[11].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[11].click();

        nodes[11].className = nodes[11].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert11);
    }
    else {
        document.removeEventListener('keypress', insert11);
    }
}
function insert12(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[12].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[12].click();

        nodes[12].className = nodes[12].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert12);
    }
    else {
        document.removeEventListener('keypress', insert12);
    }
}
function insert13(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[13].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[13].click();

        nodes[13].className = nodes[13].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert13);
    }
    else {
        document.removeEventListener('keypress', insert13);
    }
}
function insert14(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[14].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[14].click();

        nodes[14].className = nodes[14].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert14);
    }
    else {
        document.removeEventListener('keypress', insert14);
    }
}
function insert15(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[15].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[15].click();

        nodes[15].className = nodes[15].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert15);
    }
    else {
        document.removeEventListener('keypress', insert15);
    }
}
function insert16(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[16].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[16].click();

        nodes[16].className = nodes[16].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert16);
    }
    else {
        document.removeEventListener('keypress', insert16);
    }
}
function insert17(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[17].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[17].click();

        nodes[17].className = nodes[17].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert17);
    }
    else {
        document.removeEventListener('keypress', insert17);
    }
}
function insert18(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[18].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[18].click();

        nodes[18].className = nodes[18].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert18);
    }
    else {
        document.removeEventListener('keypress', insert18);
    }
}
function insert19(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[19].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[19].click();

        nodes[19].className = nodes[19].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert19);
    }
    else {
        document.removeEventListener('keypress', insert19);
    }
}
function insert20(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[20].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[20].className = nodes[20].className.replace(/(?:^|\s)marked(?!\S)/g, '');


        if (empfaenger.classList.contains("marked")){

            //remove
            empfaenger.className = empfaenger.className.replace(/(?:^|\s)marked(?!\S)/g, '');

            //add
            iban.className += " marked";

            iban.click();
        }
        else if (iban.classList.contains("marked")){

            //remove
            iban.className = iban.className.replace(/(?:^|\s)marked(?!\S)/g, '');

            //add
            betrag.className += " marked";

            betrag.click();
        }
        else if (betrag.classList.contains("marked")){

            //remove
            betrag.className = betrag.className.replace(/(?:^|\s)marked(?!\S)/g, '');

            //add
            verwendung.className += " marked";

            verwendung.click();
        }
        else if (verwendung.classList.contains("marked")){

            //remove
            verwendung.className = verwendung.className.replace(/(?:^|\s)marked(?!\S)/g, '');

            //add
            ausfuehrung.className += " marked";

            ausfuehrung.click();
        }
        else if (ausfuehrung.classList.contains("marked")){

            //remove
            ausfuehrung.className = ausfuehrung.className.replace(/(?:^|\s)marked(?!\S)/g, '');

            //add
            uebernehmen.className += " marked";

            uebernehmen.focus();

        }

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert20);
    }
    else {
        document.removeEventListener('keypress', insert20);
    }
}
function insert21(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[21].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[21].click();

        nodes[21].className = nodes[21].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert21);
    }
    else {
        document.removeEventListener('keypress', insert21);
    }
}
function insert22(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[22].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[22].click();

        nodes[22].className = nodes[22].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert22);
    }
    else {
        document.removeEventListener('keypress', insert22);
    }
}
function insert23(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[23].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[23].click();

        nodes[23].className = nodes[23].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert23);
    }
    else {
        document.removeEventListener('keypress', insert23);
    }
}
function insert24(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[24].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[24].click();

        nodes[24].className = nodes[24].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert24);
    }
    else {
        document.removeEventListener('keypress', insert24);
    }
}
function insert25(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[25].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[25].click();

        nodes[25].className = nodes[25].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert25);
    }
    else {
        document.removeEventListener('keypress', insert25);
    }
}
function insert26(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[26].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[26].click();

        nodes[26].className = nodes[26].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert26);
    }
    else {
        document.removeEventListener('keypress', insert26);
    }
}
function insert27(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[27].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[27].click();

        nodes[27].className = nodes[27].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert27);
    }
    else {
        document.removeEventListener('keypress', insert27);
    }
}
function insert28(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[28].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[28].click();

        nodes[28].className = nodes[28].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert28);
    }
    else {
        document.removeEventListener('keypress', insert28);
    }
}
function insert29(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[29].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[29].click();

        nodes[29].className = nodes[29].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert29);
    }
    else {
        document.removeEventListener('keypress', insert29);
    }
}
function insert30(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[30].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[30].click();

        nodes[30].className = nodes[30].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert30);
    }
    else {
        document.removeEventListener('keypress', insert30);
    }
}
function insert31(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[31].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[31].click();

        nodes[31].className = nodes[31].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert31);
    }
    else {
        document.removeEventListener('keypress', insert31);
    }
}
function insert32(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[32].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[32].click();

        nodes[32].className = nodes[32].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert32);
    }
    else {
        document.removeEventListener('keypress', insert32);
    }
}
function insert33(e){

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[33].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[33].click();

        nodes[33].className = nodes[33].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert33);
    }
    else {
        document.removeEventListener('keypress', insert33);
    }

}
function insert34(e){

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[34].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[34].click();

        nodes[34].className = nodes[34].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        scanKeyboardLayer1();

        document.removeEventListener('keypress', insert34);
    }
    else {
        document.removeEventListener('keypress', insert34);
    }

}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////


function tanInsert0(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[0].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[0].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert0);
    }
    else {
        document.removeEventListener('keypress', tanInsert0);
    }
}
function tanInsert1(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[1].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[1].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert1);
    }
    else {
        document.removeEventListener('keypress', tanInsert1);
    }
}
function tanInsert2(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[2].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[2].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert2);
    }
    else {
        document.removeEventListener('keypress', tanInsert2);
    }
}
function tanInsert3(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[3].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[3].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert3);
    }
    else {
        document.removeEventListener('keypress', tanInsert3);
    }
}
function tanInsert4(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[4].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[4].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert4);
    }
    else {
        document.removeEventListener('keypress', tanInsert4);
    }
}
function tanInsert5(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[5].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[5].className = nodes[5].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        nodes[5].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert5);
    }
    else {
        document.removeEventListener('keypress', tanInsert5);
    }
}
function tanInsert6(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[6].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[6].className = nodes[6].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        nodes[6].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert6);
    }
    else {
        document.removeEventListener('keypress', tanInsert6);
    }
}
function tanInsert7(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[7].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[7].className = nodes[7].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        nodes[7].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert7);
    }
    else {
        document.removeEventListener('keypress', tanInsert7);
    }
}
function tanInsert8(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[8].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[8].className = nodes[8].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        nodes[8].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert8);
    }
    else {
        document.removeEventListener('keypress', tanInsert8);
    }
}
function tanInsert9(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[9].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[9].className = nodes[9].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        nodes[9].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert9);
    }
    else {
        document.removeEventListener('keypress', tanInsert9);
    }
}
function tanInsert10(e) {

    checkKeyboards();
    nodePicker();

    cycleStart = 0;

    if ((e.keyCode === 13) && (nodes[10].classList.contains("marked"))) {
        clearTimeout(timeout);

        nodes[10].className = nodes[10].className.replace(/(?:^|\s)marked(?!\S)/g, '');

        nodes[10].click();

        scanLayer6UeberweisungSubmitKeyboard();

        document.removeEventListener('keypress', tanInsert10);
    }
    else {
        document.removeEventListener('keypress', tanInsert10);
    }
}